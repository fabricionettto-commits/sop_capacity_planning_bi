# Reports

Generated demo reports are written here when the scripts in `src/` are executed.

The repository keeps this folder, but ignores generated `.html`, `.xlsx`, and `.csv` outputs so the public project stays clean.

